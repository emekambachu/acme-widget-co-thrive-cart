{
  "name": "acme-widget-co/thrivecart",
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    }
  },
  "require": {
    "php": "^8.0 || ^8.2",
    "ext-json": "*"
  },
  "require-dev": {
    "phpunit/phpunit": "^9.5",
    "phpstan/phpstan": "^1.7"
  },
  "scripts": {
    "test": "phpunit --colors=always",
    "phpstan": "phpstan analyse --level=max app"
  }
}
